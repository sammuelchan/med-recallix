{
  "$schema": "https://pages.edgeone.ai/schema/edgeone.json",
  "name": "med-recallix",
  "install": {
    "command": "PNPM_CONFIG_DANGEROUSLY_ALLOW_ALL_BUILDS=true pnpm install"
  },
  "build": {
    "command": "cp .env.example .env && pnpm run build",
    "output": ".next"
  }
}
