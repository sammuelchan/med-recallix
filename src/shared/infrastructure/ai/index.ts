/** Barrel — re-exports the AI client factory and configuration manager. */
export { createAIClient } from "./ai.client";
export { getAIConfig, setAIConfig } from "./ai.config";
export type { AIConfig } from "./ai.config";
